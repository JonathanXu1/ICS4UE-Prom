import javax.swing.*;
public class LoadProjectLayout extends CustomPanel{
  public LoadProjectLayout(int x, int y) {
      super(x, y, "Dashboard", "Open past projects.");
      JTextField projectName = new JTextField();

  }
}